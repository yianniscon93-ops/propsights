---
description: Run a SQL query against bnb.duckdb and return results
---

Run the following SQL query against the local DuckDB database at `/Users/yiannisconstantinides/vscode/bnb/bnb.duckdb` using the Bash tool with `python3 -c "import duckdb; ..."`. If no query is provided in $ARGUMENTS, show a summary of all tables and their row counts. Format results clearly. If the database doesn't exist locally, note that the user may need to download it from the server first (scp root@204.168.209.175:/opt/bnb_git/bnb.duckdb ~/vscode/bnb/bnb.duckdb).

Query to run: $ARGUMENTS
