# Serving-layer Postgres (schema owned by data engineering — read-only).
# When unset, the dashboard serves deterministic demo data and shows a
# "DEMO DATA" badge.
#
# Local dev — Postgres on the Hetzner box is localhost-only, so tunnel first:
#   ssh -N -L 5433:localhost:5432 root@204.168.209.175
# then:
# DATABASE_URL=postgresql://bnb:bnb@localhost:5433/bnb
